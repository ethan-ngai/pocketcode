/**
 * @file admin.functions.ts
 * @description Admin server function boundary for dashboard data.
 * @module admin
 */

/**
 * Placeholder admin data handler.
 * @returns Empty array until database-backed admin queries are implemented.
 * @remarks Admin routes should call feature functions here instead of embedding
 * database queries in TanStack route components.
 */
export async function listAdminPlaceholders(): Promise<unknown[]> {
  return [];
}
